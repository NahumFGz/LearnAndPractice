version: '3.8'

services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    image: mi-backend:1.0
    # command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - 8000:8000
    depends_on:
      - postgres